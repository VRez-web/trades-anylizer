{
  "name": "trades-anylizer",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "db:push": "node --dns-result-order=ipv4first --env-file=.env ./node_modules/drizzle-kit/bin.cjs push",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "drizzle-orm": "^0.45.2",
    "lightweight-charts": "^5.1.0",
    "nuxt": "^4.4.2",
    "postgres": "^3.4.7",
    "undici": "^8.1.0",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
